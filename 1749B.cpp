#include<bits/stdc++.h>
using namespace std;
void solve(){
    int n;
    cin>>n;
    vector<int> a(n);
    vector<int> b(n);
    for(auto& ai:a)
        cin>>ai;
    for(auto& bi:b)
        cin>>bi;
}
int main(){
    int t;
    cin>>t;
    while(t--){
        solve();
    }
}